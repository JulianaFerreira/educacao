## How to use?

```
Clone this repository
Make sure you have installed the packages in requirements.txt (pip install -r requirements.txt)
run `python generate_probabilities.py`
run `python simulation.py`
```
